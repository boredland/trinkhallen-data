# Auto-request @boredland on every PR so the github-actions[bot] PRs
# (bulk gmaps payment, daily cron, osm enrich) generate a "review
# requested" notification instead of landing silently.
* @boredland
